{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hanger",
  "description": "Theme hanger window permissions",
  "windows": ["hanger"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-start-dragging"
  ]
}
